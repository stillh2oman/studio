/**
 * Primary calendar for scheduling + Home Google sync.
 * Use `primary` so the Calendar API targets the authenticated account’s main calendar.
 * Override with env GOOGLE_CALENDAR_ID (e.g. shared calendar id) if needed.
 */
export const DEFAULT_GOOGLE_CALENDAR_ID = 'primary';

export const GOOGLE_CALENDAR_TIME_ZONE = 'America/Chicago';
