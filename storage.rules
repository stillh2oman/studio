rules_version = '2';
// Deploy with: firebase deploy --only storage
// Timesheet PDFs uploaded from the app under timesheet_reports/{firmId}/...
service firebase.storage {
  match /b/{bucket}/o {
    match /timesheet_reports/{firmId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
